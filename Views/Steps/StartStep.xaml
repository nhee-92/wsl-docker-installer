<views:BaseStep x:Class="wsl_docker_installer.Views.Steps.StartStep"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:wsl_docker_installer.Views.Steps">
    <StackPanel Orientation="Vertical" >
        <TextBlock 
            Text="This wizard makes Docker natively available on Windows systems so that no additional software is required to use Docker."
            TextWrapping="Wrap"
            FontSize="11" 
            Margin="10 5 0 0"
        />
        <TextBlock
            Text="WSL2 is installed, if not available, a light-weight Ubuntu distribution and Docker in the VM. In addition, the VM is made accessible from Windows via a TCP port."
            TextWrapping="Wrap"
            FontSize="11"
            Margin="10 5 0 0"/>
    </StackPanel>
</views:BaseStep>